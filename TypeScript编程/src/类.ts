/**
 * super
 * 1.父类方法调用 super.take()
 * 2.构造方法调用 super() 只能在子类构造函数中调用
 */

/**
 * 以this为返回类型
 * 用于链式调用
 */

/**
 * 类型别名和接口的区别
 * 1.类型别名右边可以是类型表达式（类型和类型运算符&｜），接口只能是结构
 * 2.扩展接口（extends）时会检查可赋值性
 * 3.接口会声明合并
 */

/**
 * 实现接口（implements）还是扩展抽象类（extends）
 * 1.接口是对结构建模，抽象类只能对类建模
 * 2.接口只存在编译时，不生成js代码，抽象类存在运行时，生成js代码
 */

/**
 * 构造方法中不能声明泛型
 * 静态方法不能访问类的泛型
 */

/**
 * 私有的构造函数（private constructor）
 * 禁止继承、禁止实例化
 * 可应用于单例模式（提供create方法生成实例）
 */
